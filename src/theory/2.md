Типы данных.

Integer,  int.
могут быть знаковые signed и беззнаковые unsigned.
Есть int с длинной в бит, который зависит от реализации. Обычно 32 бита или 4 байта. Но бывает и 16 бит. 

char (8 bits)
этот тип для символов ASCII 

есть и типы для чисел с плавающей запятой.
float
double
long double


bool логический тип ( true или false)
1 - true
0 - false 

дальше у нас есть такой типа как "string класс" 
string



<string>  это заголовочный файл, который даёт тебе тип
std::string.



Указатели. Pointers
 -это специальный тип данных для адресов памяти.
 Указатели могут выступать в качестве ссылок на существующие переменные и один и тот же указатель может быть использован для последовательного доступа ко многим различным переменным 